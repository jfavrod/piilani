// eslint-disable-next-line no-shadow
export enum DBDriver {
    postgres = 'pg',
}

// eslint-disable-next-line no-shadow
export enum Env {
    dev = 'dev',
    'non-prod' = 'non-prod',
    prod = 'prod',
}
